server:
  port: 8888

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/bank
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
