server:
  port: 9092

spring:
  application:
    name: credit-service
  datasource:
    url: jdbc:mysql://localhost:3306/bank
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect


